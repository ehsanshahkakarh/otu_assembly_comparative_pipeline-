# Python dependencies for 18S Census Parser
# Install with: pip install -r requirements.txt

# Core dependencies
pandas>=1.3.0

# Note: taxonkit is a command-line tool and must be installed separately
# See REPRODUCIBILITY_GUIDE.md for taxonkit installation instructions

